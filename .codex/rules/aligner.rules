# 이 파일은 harness/hooks/policies/permissions.json 에서 생성됩니다.
# 직접 고치지 마세요. 다시 생성: python3 scripts/harness/generate.py

prefix_rule(
    pattern = ["git", "status"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "diff"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "log"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "show"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "branch"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "fetch"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "switch"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "add"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "rev-parse"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "commit"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "push"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "rebase"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "create"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "issue", "list"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "issue", "view"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "list"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "view"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "diff"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "status"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "repo", "view"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "auth", "status"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["./gradlew", "ktlintCheck"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["./gradlew", "ktlintFormat"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["./gradlew", "build"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["./gradlew", "test"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["./gradlew", "integrationTest"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["./gradlew", "projects"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["./gradlew", "dependencies"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["python3", "scripts/harness/generate.py"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["python3", "scripts/harness/validate.py"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["rg"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["fd"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["ls"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["tree"],
    decision = "allow",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "ready"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "merge"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "issue", "create"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "review"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "comment"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "pr", "edit"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["gh", "api"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "rebase", "--abort"],
    decision = "prompt",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "reset", "--hard"],
    decision = "forbidden",
    justification = "Aligner 저장소 공통 명령 정책",
)

prefix_rule(
    pattern = ["git", "clean", "-fd"],
    decision = "forbidden",
    justification = "Aligner 저장소 공통 명령 정책",
)
