# Windows 배치 파일은 CRLF 여야 한다. LF 면 cmd 파서가 GOTO/CALL 라벨을 잘못 읽어
# gradlew.bat 이 Windows 에서 깨진다.
*.bat text eol=crlf

# Unix 스크립트는 반대로 LF 를 강제한다. core.autocrlf 설정이 제각각인 팀에서
# CRLF 로 체크아웃되면 shebang 이 깨진다.
*.sh text eol=lf
gradlew text eol=lf
